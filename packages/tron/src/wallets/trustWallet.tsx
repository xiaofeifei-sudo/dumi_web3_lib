import { metadata_Trust } from 'pelican-web3-lib-assets';
import type { WalletMetadata } from 'pelican-web3-lib-common';

export const TrustWallet: WalletMetadata = {
  ...metadata_Trust,
  key: 'trustTronWallet',
  group: 'Popular',
};
