import { metadata_imToken } from 'pelican-web3-lib-assets';
import type { WalletMetadata } from 'pelican-web3-lib-common';

export const ImTokenWallet: WalletMetadata = {
  ...metadata_imToken,
  key: 'imtokenTronWallet',
  group: 'Popular',
};
